export const CLAUDE_SETUP_STEPS = [
  'Download the `Claude Skills` file from this page. It uses the same live archive content as `index.md`, but exports it as `CLAUDE.md` for Claude Code.',
  'If you do not already have Claude Code installed, install it with `npm install -g @anthropic-ai/claude-code`.',
  'Move the downloaded `CLAUDE.md` file into the root of the project or folder where you want Claude Code to use the Hendrix archive.',
  'Open that folder in your terminal and run `claude`.',
  'Claude Code will automatically read `CLAUDE.md` from the project root as instruction and context material.',
  'Start chatting or asking Claude to use the Hendrix archive when it answers, rewrites, or helps with decisions.',
];
